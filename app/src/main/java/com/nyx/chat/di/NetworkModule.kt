package com.nyx.chat.di

/**
 * Red Team AI v3.1.0
 *
 * Network is now built DYNAMICALLY inside ChatRepository per provider base URL.
 * This module is intentionally empty — kept so Hilt component graph stays valid.
 * If you want a singleton OkHttpClient for shared use later, add it here.
 */
